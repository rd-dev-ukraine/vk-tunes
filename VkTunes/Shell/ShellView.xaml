<Window x:Class="VkTunes.Shell.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VkTunes.Shell"
        mc:Ignorable="d"
        Title="VK-Tunes">
    <Grid>
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" TextAlignment="Center" FontSize="30">VK-Tunes</TextBlock>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <Button x:Name="MyAudio">My Audio</Button>
                <Button x:Name="SearchAudio">Search audio</Button>
            </StackPanel>
            <ContentControl x:Name="Progress" DockPanel.Dock="Top" Margin="10">
            </ContentControl>
            <DockPanel Margin="10">
                <ContentControl x:Name="ActiveItem" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"></ContentControl>
            </DockPanel>
        </DockPanel>
    </Grid>
</Window>
